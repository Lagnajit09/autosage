# autobot dependencies. Pinned for reproducibility — bump deliberately
# when adopting new features (litellm in particular evolves fast).

# Web framework
fastapi==0.136.1
uvicorn[standard]==0.47.0

# Config + validation
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2

# HTTP client for Django and LLM providers
httpx==0.28.1

# Redis client for hot-context cache
redis==5.2.1

# Clerk JWT verification — [crypto] pulls in `cryptography` for RS256.
PyJWT[crypto]==2.10.1
cryptography==44.0.0

# LLM gateway. 1.55.4 → 1.74.0 fixes:
#   • Databricks-shared chunk parser crash on empty `choices` (the
#     autobot salvage path in `llm/client.py` still catches this case
#     as defense-in-depth).
#   • Pydantic v2 serializer warnings from litellm internals.
#   • Relaxes the httpx<0.28 dependency.
litellm==1.74.0

# Token counting for the summarization threshold
tiktoken==0.8.0

# Per-user rate limiting for the chat endpoint. Storage URI points at
# Redis db /2 so all uvicorn workers share counters — without that,
# `--workers 2` would silently double the effective rate limit.
slowapi==0.1.9
